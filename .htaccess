# Lindungi kode & database bila folder proyek berada di dalam document root.
<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteRule ^(app|database|tests)(/|$) - [F,L]
    RewriteRule ^(config\.php|\.git) - [F,L]
</IfModule>
